ASSIGNMENT 4

SUBQUERY

Q1.DISPLAY ALL THE EMPLOYEES WHOSE DEPARTMENT NAMES ENDING 'S'
            SELECT ENAME FROM EMPLOYE WHERE DEPTNO IN(SELECT DEPTNO FROM DEPT WHERE DNAME LIKE '%S');

Q2.QUERY TO DISPLAY THE EMPLOYEE NAMES WHO IS HAVING MAXIMUM SALARY IN DEPT NAME "ACCOUNTING"
            SELECT ENAME FROM EMPLOYE WHERE SALARY  IN(SELECT MAX(SALARY) FROM EMPLOYE WHERE DEPTNO  IN(SELECT DEPTNO FROM DEPT  WHERE DNAME='ACCOUNTING');

Q3.QUERY TO DISPLAY THE DEPT NAME WHO IS HAVING HIGHEST COMMISSION
            SELECT DNAME FROM DEPT WHERE DEPTNO IN(SELECT DEPTNO FROM EMPLOYE WHERE COMM =(SELECT MAX(COMM)
FROM EMP);

Q4. QUERY TO DISPLAY THE EMPLOYEE NAMES WHOSE NAME AS 2ND CHARACTER SHOULD BE 'O'. 4. QUERY TO DISPLAY THE EMPLOYEE NAMES WHOSE DEPARTMENT NAME HAS 2ND CHARACTER AS 'O'.
            SELECT ENAME FROM EMPLOYE WHERE DEPTNO IN(SELECT DEPTNO FROM DEPT DNAME LIKE  '_O%');

Q5. QUERY TO DISPLAY ALL THE EMPLOYEES WHO’S DEPT NUMBER IS SAME AS SCOTT.
           SELECT ENAME FROM EMPLOYE WHERE DEPTNO IN(SELECT DEPTNO FROM EMPLOYE WHERE ENAME=’SCOTT’);

Q6.QUERY TO DISPLAY ALL THE EMPLOYEES IN 'OPERATIONS AND ACCOUNTING' DEPT.
           SELECT ENAME FROM EMPLOYE WHERE DEPTNO IN(SELECT DEPTNO FROM DEPT WHERE DNAME IN(‘OPERATIONS’,’ACCOUNTING’));

Q7.LIST THE EMPLOYEES WHO HAS SALARY GREATER THAN MILLER
          SELECT ENAME FROM EMPLOYE WHERE SALARY>(SELECT SALARY FROM EMPLOYE  WHERE ENAME=’MILLER’);

Q8. LIST DEPARTMENT NAME HAVING ATLEAST 3 SALESMAN
          SELECT DNAME FROM DEPT WHERE DEPTNO IN(SELECT DEPTNO FROM EMPLOYE WHERE DESIGNATION=’SALESMAN’ GROUP BY DEPTNO HAVING COUNT(*)>=3);

Q9. DISPLAY ALL EMPLOYESS WHO DO NOT HAVE ANY REPORTER. 9. DISPLAY THE DNAME OF AN EMPLOYEES WHO HAS NO REPORTING MANAGER.
          SELECT ENAME FROM EMPLOYE WHERE NVL(MGR,0) NOT IN(SELECT EMPNO FROM EMP);

Q10. LIST ALL THE EMPLOYEES WHO ARE REPORTING TO JONES MANAGER
          SELECT ENAME FROM EMPLOYE WHERE MGR=(SELECT MGR FROM EMPLOYE WHERE ENAME=’JONES’);

Q11. LIST EMPLOYEES FROM RESEARCH&ACCOUNTING HAVING AT LEAST 2 REPORTING.
           SELECT ENAME FROM EMPLOYE WHERE DEPTNO IN(SELECT DEPTNO FROM DEPT WHERE DNAME IN(‘RESEARCH’,’ACCOUNTING’) AND EID IN(SELECT MGR FROM EMPLOYE GROUP BY MGR HAVING COUNT(*)>=2));

Q12. DISPLAY THE DEPARTMENT NAME OF THE EMPLOYEE WHOSE NAME DOES NOT STARTS WITH S AND SALARY BETWEEN 1500 TO 3000.
          SELECT DNAME FROM DEPT WHERE DEPTNO IN(SELECT DEPTNO FROM EMPLOYE WHERE ENAME NOT LIKE 'S%' AND SALARY BETWEEN 1500 AND 3000);

Q13.DISPLAY LOCATION OF EMPLOYEE WHOSE SALARY IS MINIMUM SALARY BUT SALARY IS GREATER THAN 2000
          SELECT LOCATION FROM DEPT WHERE DEPTNO IN(SELECT DEPTNO FROM EMPLOYE WHERE SALARY IN(SELECT MIN(SALARY) FROM EMPLOYE WHERE SALARY>2000));

Q14. DISPLAY THE LOCATION OF AN EMPLOYEE IN ACCOUNTING DEPARTMENT.
           SELECT LOCATION FROM DEPT WHERE DEPTNO IN (SELECT DEPTNO FROM EMPLOYE WHERE DEPTNO IN (SELECT DEPTNO FROM DEPT WHERE DNAME='ACCOUNTING'));

Q15. DISPLAY THE DEPARTMENT LOCATION THAT IS HAVING GREATER THAN FOUR EMPLOYEES IN IT.
          SELECT LOCATION FROM DEPT WHERE DEPTNO IN(SELECT DEPTNO FROM EMPLOYE GROUP BY DEPTNO HAVING COUNT(*)>4);

Q16. WRITE A QUERY TO DISPLAY ALL THE EMPLOYEE WHOSE JOB NOT SAME AS ALLEN AND SALARY IS GREATER THAN MARTIN.
          SELECT ENAME FROM EMPLOYE WHERE DESIGNATION NOT IN(SELECT DESIGNATION FROM EMPLOYE WHERE ENAME ='ALLEN' AND SALARY>( SELECT SALARY FROM EMPLOYE WHERE ENAME=’MARTIN’));

Q17. DISPLAY ALL THE EMPLOYEES WHO IS HAVING LOCATION IS SAME AS ADAM'S MANAGER?
         SELECT ENAME FROM EMPLOYE WHERE DEPTNO IN (SELECT DEPTNO FROM DEPT WHERE LOCATION IN (SELECT LOCATION FROM DEPT WHERE DEPTNO IN (SELECT DEPTNO FROM EMPLOYE WHERE EMPNO IN (SELECT MGR FROM EMPLOYE WHERE ENAME='ADAMS'))));

Q18. DISPLAY THE JOB, MANAGER NUMBER OF EMPLOYEES WHO IS WORKING FOR JONES?
         SELECT DESIGNATION FROM EMPLOYE WHERE MGR=(SELECT EMPNO FROM EMPLOYE WHERE ENAME=’JONES’);

Q19. DISPLAY THE EMPLOYEE NAMES, HIGHER DATE, COMMISSION OF FORD'S MANAGER?
         SELECT ENAME,HIREDATE,COMM FROM EMPLOYE WHERE EID=(SELECT MGR FROM EMP WHERE ENAME=’FORD’);

Q20. DISPLAY THE NUMBER OF EMPLOYEES WHO ARE GETTING SALARY LESS THAN THE BLAKE'S MANAGER
         SELECT COUNT(*) FROM EMPLOYE WHERE SALARY<(SELECT SALARY FROM EMPLOYE WHERE EID=(SELECT MGR FROM EMPLOYE WHERE ENAME=’BLAKE’));

Q21. LIST EMPLOYEES WHO LOCATED IN CHICAGO AND THEIR COMMISSION IS ZERO.
         SELECT ENAME FROM EMPLOYE WHERE DEPTNO=(SELECT DEPTNO FROM DEPT WHERE LOCATION=’CHICAGO’) AND COMM =0;

Q22. LIST EMPLOYEES WHO WORK FOR SALES DEPARTMENT AND THEIR SALARY GREATER THAN AVERAGE SALARY OF THEIR DEPARTMENT.
         SELECT ENAME FROM EMPLOYE WHERE DEPTNO=(SELECT DEPTNO FROM DEPT WHERE DNAME='SALES') AND SALARY>(SELECT
AVG(SALARY) FROM EMPLOYE WHERE DEPTNO IN (SELECT DEPTNO FROM DEPT WHERE DNAME=’SALES’));

Q23. LIST EMPLOYEES WHO ARE WORKING IN RESEARCH DEPARTMENT AND THEY ARE MANAGER.
          SELECT ENAME FROM EMPLOYE WHERE DEPTNO=(SELECT DEPTNO FROM DEPT WHERE DNAME='RESEARCH') AND DESIGNATION='MANAGER';

Q24. DISPLAY DEPARTMENT NAME OF THE EMPLOYEES WHO EARN COMMISSION.
          SELECT DNAME FROM DEPT WHERE DEPTNO=(SELECT DEPTNO FROM EMPLOYE WHERE COMM IS NOT NULL);

Q25. DISPLAY DEPARTMENT NAME OF THE EMPLOYEE WHO EARN MAXIMUM SALARY AND HAVE NO REPORTING MANAGER.
          SELECT DNAME FROM DEPT WHERE DEPTNO IN(SELECT DEPTNO FROM EMPLOYE WHERE SALARY IN (SELECT MAX(SALARY) FROM EMP) AND MGR IS NULL);

Q26. DISPLAY EMPLOYEE DETAILS WHO ARE REPORTING TO BLAKE AND HAVE COMMISSION WITHOUT USING NULL OR NOT NULL
          SELECT * FROM EMPLOYE WHERE MGR=(SELECT EID FROM EMPLOYE WHERE ENAME='BLAKE') AND COMM >=0;

Q27. LIST ALL THE DEPT NAME AND LOC OF ALL THE SALESMAN MANAGER-MANAGER'S
          SELECT DNAME,LOCATION FROM DEPT WHERE DEPTNO IN(SELECT DEPTNO FROM EMPLOYE WHERE EID IN(SELECT MGR FROM EMPLOYE WHERE EID IN(SELECT MGR FROM EMPLOYE WHERE DESIGNATION='SALESMAN')));

Q28. LIST THE EMPLOYEE DEPT NAME AND LOC OF ALL THE EMPLOYEES WHO ARE CLERK ,REPORTING TO BLAKE AND SALARY IS LESSER THAN MARTIN SALARY
          SELECT DNAME,LOCATION FROM DEPT WHERE DEPTNO IN(SELECT DEPTNO FROM EMPLOYE WHERE DESIGNATION =’CLERK’) AND MGR IN (SELECT EID FROM EMPLOYE WHERE ENAME =’BLAKE’) AND SALARY<(SELECT SALARY FROM EMPLOYE WHERE ENAME =’MARTIN’);

Q29. LIST THE EMPLOYEES WHO DOES NOT DIRECTLY REPORT TO PRESIDENT,HAVE COMMISSION AND SALARY MORE THAN MAX SALARY OF ALL THE CLERK WITHOUT USING NULL OR NOT NULL
          SELECT ENAME FROM EMPLOYE WHERE MGR IN(SELECT EID FROM EMPLOYE WHERE MGR IN(SELECT EMPNO FROM EMPLOYE WHERE DESIGNATION='PRESIDENT')) AND COMM >=0 AND SALARY>(SELECT MAX(SALARY) FROM EMPLOYE WHERE DESIGNATION='CLERK');

Q30. LIST THE EMPLOYEES WHO JOINED AFTER 2 YEARS OF FIRST EMPLOYEE OF THE COMPANY AND MORE THAN BLAKE SALARY
          SELECT ENAME FROM EMPLOYE WHERE HIREDATE>(SELECT MIN(HIREDATE)+365*2 FROM EMPLOYE) AND SALARY>(SELECT SALARY FROM EMPLOYE WHERE ENAME='BLAKE');

Q31. DISPLAY LOCATION OF ALL THE EMPLOYEES WHO REPORTING TO BLAKE
         SELECT LOCATION FROM DEPT WHERE DEPTNO IN(SELECT DEPTNO FROM EMPLOYE WHERE MGR IN(SELECT EMPNO
FROM EMPLOYE WHERE ENAME='BLAKE'));

Q32. LIST ALL THE EMPLOYEES WHOSE JOB IS SAME AS JONES AND THEIR SALARY LESSER THAN SCOTT
          SELECT ENAME FROM EMPLOYE WHERE DESIGNATION=(SELECT DESIGNATION FROM EMPLOYE WHERE ENAME='JONES') AND SALARY<(SELECT SALARY FROM EMPLOYE WHERE ENAME='SCOTT');

Q33.DISPLAY ALL THE EMPLOYEES OF DEPARTMENT 30, 20 WITH THERE ANUAL SALARY AND HAVING AT LEAST 3 EMPLOYEES
         SELECT ENAME,SALARY*12 ANNUAL_SALARY  FROM EMPLOYE WHERE DEPTNO IN ( SELECT DEPTNO FROM EMPLOYE WHERE DEPTNO IN(30,20)GROUP BY DEPTNO HAVING COUNT(*)>=3);

Q34.DISPLAY ALL THE EMPLOYEES WHO ARE EARN LESS THAN ANY OF THE SALESMAN?
         SELECT ENAME FROM EMPLOYE WHERE SALARY < ANY (SELECT SALARY FROM EMPLOYE WHERE DESIGNATION ='SALESMAN');

Q35.DISPLAY ALL THE EMPLOYEES WHO ARE JOINED BEFORE THE LAST PERSON?
         SELECT ENAME FROM EMPLOYE WHERE HIREDATE<(SELECT MAX(HIREDATE) FROM EMPLOYE);

Q35.FIND 3RD MINIMUM SALARY IN THE EMPLOYEE TABLE.
          SELECT MIN(SALARY) FROM EMPLOYE WHERE SALARY>(SELECT MIN(SALARY) FROM EMPLOYE WHERE SALARY>(SELECT MIN(SALARY) FROM EMPLOYE));

QQ36.DISPLAY ALL THE EMPLOYEES WHO ARE EARNING MORE THAN ANY OF THE MANAGER.
          SELECT ENAME FROM EMPLOYE WHERE SALARY > ANY(SELECT SALARY FROM EMPLOYE WHERE DESIGNATION='MANAGER');

Q37.LIST EMPLOYEES WHO JOINED AFTER 4 YEARS OF 1ST EMPLOYEE OF THE COMPANY AND LESS THAN BLAKE SALARY.
           SELECT ENAME FROM EMPLOYE WHERE HIREDATE>(SELECT MIN(HIREDATE)+365*4 FROM EMPLOYE) AND SALARY<(SELECT SALARY  FROM EMPLOYE WHERE ENAME='BLAKE');

Q38. DISPLAY THE DEPARTMENT INFORMATION OF EMPLOYEE WHO IS WORKING FOR NEW YORK LOCATION
            SELECT * FROM DEPT WHERE DEPTNO IN (SELECT DEPTNO FROM EMPLOYE WHERE DEPTNO IN (SELECT DEPTNO FROM DEPT WHERE LOCATION = 'NEW YORK'));

Q39.DISPLAY LOCATION OF EMPLOYEES, WHOSE NAME DOESN'T START WITH A AND SALARY BETWEEN 1000 AND 3000.
           SELECT LOCATION FROM DEPT WHERE DEPTNO IN(SELECT DEPTNO FROM EMPLOYE WHERE ENAME NOT LIKE 'A%' AND SALARY BETWEEN 1000 AND 3000);

Q40. DISPLAY DEPARTMENT NAME OF ALL THE EMPLOYEES WHO ARE REPORTING TO BLAKE.
            SELECT DNAME FROM DEPT WHERE DEPTNO IN(SELECT DEPTNO FROM EMPLOYE WHERE MGR IN(SELECT EID
FROM EMPLOYE WHERE ENAME='BLAKE'));

Q41. DISPLAY MARTIN'S MANAGER'S MANAGER'S DEPARTMENT NAME AND LOCATION.
             SELECT DNAME,LOCATION FROM DEPT WHERE DEPTNO IN (SELECT DEPTNO FROM EMPLOYE WHERE EID IN (SELECT MGR
FROM EMPLOYE WHERE EID IN(SELECT MGR FROM EMP WHERE ENAME='MARTIN')));

Q 42.DISPLAY THE MANAGER NUMBER,JOB AND DEPARTMENT NUMBER FOR THOSE WHO DON'T HAVE COMMISSION IN THE LOCATION CHICAGO OR DALLAS
            SELECT MGR,DESIGNATION,DEPTNO FROM EMPLOYE WHERE COMM IS NULL;

Q43.DISPLAY THE EMPLOYEE DETAILS WITH THEIR ANNUAL SALARY WHO EARN MAXIMUM COMMISSION
            SELECT SALARY,SALARY*12 ANNUAL_SALARY FROM EMPLOYE WHERE COMM IN (SELECT MAX(COMM)
FROM EMPLOYE);

Q44.DISPLAY ALL THE EMPLOYEE WHOSE DEPARTMENT IS SALES AND WHO IS EARNING SOME COMMISSION (I.E COMMISSION IS NOT NULL OR ZERO)AND WHO IS HIRED BEFORE THE LAST PERSON HIRED.
           SELECT ENAME FROM EMPLOYE WHERE DEPTNO IN (SELECT DEPTNO FROM DEPT WHERE DNAME='SALES') AND COMM >=0 AND HIREDATE<(SELECT MAX(HIREDATE) FROM EMPLOYE);

Q45.DISPLAY ALL THE DEPARTMENT NAMES FOR WARD'S MANAGER'S MANAGER
           SELECT DNAME FROM DEPT WHERE DEPTNO IN (SELECT DEPTNO FROM EMPLOYE WHERE EID IN(SELECT MGR FROM EMPLOYE WHERE EID IN(SELECT MGR FROM EMPLOYE WHERE ENAME='WARD')));

Q46:DISPLAY DEPARTMENT NAMES OF EMPLOYEE'S WHOSE SALARY IS GREATER THAN AVERAGE SALARY OF ALL THE CLERK'S
           SELECT DNAME FROM DEPT WHERE DEPTNO IN (SELECT DEPTNO FROM EMPLOYE WHERE SALARY>(SELECT AVG(SALARY) FROM EMPLOYE WHERE DESIGNATION='CLERK'));

Q47.DISPLAY THE LAST EMPLOYEE RECORD WITH 25% HIKE IN SALARY.
            SELECT SALARY,SALARY+SALARY*25/100 HIKE FROM EMPLOYE WHERE HIREDATE IN (SELECT MAX(HIREDATE) FROM EMPLOYE);

Q48.DISPLAY THE DEPARTMENT NUMBER WHO WORKING IN SALES DEPARTMENT AND THEY ARE MANAGER.
            SELECT DEPTNO FROM EMPLOYE WHERE DEPTNO IN(SELECT DEPTNO FROM DEPT WHERE DNAME='SALES') AND DESIGNATION='MANAGER';

Q49.DISPLAY DEPARTMENT NAME OF THE EMPLOYEE WHO EARN MINMUM SALARY AND HAVE REPORTING MANAGER.
             SELECT DNAME FROM DEPT WHERE DEPTNO IN(SELECT DEPTNO FROM EMPLOYE WHERE SALARY IN(SELECT MIN(SALARY) FROM EMPLOYE WHERE MGR IS NOT NULL));

Q50.DISPLAY HIREDATE AND JOB OF ALL THE EMPLOYEES WORKING FOR SALES
            SELECT HIREDATE,DESIGNATION FROM EMPLOYE WHERE DEPTNO IN(SELECT DEPTNO FROM DEPT WHERE DNAME='SALES');

Q51.DISPLAY LOCATION AND DNAME OF EMPLOYEE WHO WORKING AS PRESIDENT
             SELECT LOCATION,DNAME FROM DEPT WHERE DEPTNO IN(SELECT DEPTNO FROM EMPLOYE WHERE DESIGNATION='PRESIDENT');

Q52.DISPLAY THE DNAME OF EMPLOYEES WHOES SALARY IS MAXIMUM SALARY BUT LESSER THAN 3000
             SELECT DNAME FROM DEPT WHERE DEPTNO IN(SELECT DEPTNO FROM EMPLOYE WHERE SALARY IN(SELECT MAX(SALARY) FROM EMPLOYE WHERE SALARY<3000));

Q53.DISPLAY THE DEPARTMENT NAME WHO ARE REPORTING TO ADAMS.
             SELECT DNAME FROM DEPT WHERE DEPTNO IN(SELECT DEPTNO FROM EMPLOYE WHERE MGR IN(SELECT EID FROM EMPLOYE
WHERE ENAME='ADAMS'));

Q54.DISPLAY LAST EMPLOYEE RECORD ACCORDING TO EMPNO.
             SELECT * FROM EMPLOYE WHERE EID IN(SELECT MAX(EID) FROM EMPLOYE);

Q55.DISPLAY ALL THE EMPLOYEE WHOSE SALARY IS GREATER THAN AVERAGE SALARY OF DEPARTMENT 30.
            SELECT ENAME FROM EMPLOYE WHERE SALARY>(SELECT AVG(SALARY) FROM EMPLOYE WHERE DEPTNO=30);

Q56.DISPLAY THE NUMBER OF EMPLOYEES WHO WORK FOR RESEARCH DEPT AND THEIR SALARY IS LESSER THAN ONE OF THE SALARY IN DEPARTMENT IN 10.
            SELECT COUNT(*) FROM EMPLOYE WHERE DEPTNO IN(SELECT DEPTNO FROM DEPT WHERE DNAME='RESEARCH') AND SALARY<(SELECT MAX(SALARY) FROM EMPLOYE WHERE DEPTNO=10);

Q57.DISPLAY THE DNAME THAT ARE HAVING CLERK IN IT.
            SELECT DNAME FROM DEPT WHERE DEPTNO IN(SELECT DEPTNO FROM EMPLOYE WHERE DESIGNATION='CLERK');

Q58.DISPLAY THE DEPARTMENT NAMES THAT ARE HAVING ATLEAST ONE L IN IT.
            SELECT DNAME FROM DEPT WHERE DNAME LIKE '%L%';

Q59. DISPLAY ALL THE EMPLOYEES WHO ARE JOINED AFTER BLAKE.
            SELECT ENAME FROM EMPLOYE WHERE HIREDATE>(SELECT HIREDATE FROM EMPLOYE WHERE ENAME='BLAKE');

Q60. LIST THE DEPT NAME THAT ARE HAVING AT LEAST 3 EMPLOYEES BUT NOT MORE THAN 5 EMPLOYEES IN IT.
             SELECT DNAME FROM DEPT WHERE DEPTNO IN (SELECT DEPTNO FROM EMPLOYE GROUP BY DEPTNO HAVING COUNT(*)>=3 AND COUNT (*)>5);

Q61. DISPLAY THE LOCATION OF ALL EMPLOYEES WHOSE REPORTING MANAGER SALARY IS GREATER THAN 2000.
             SELECT LOCATION FROM DEPT WHERE DEPTNO IN ( SELECT DEPTNO FROM EMPLOYE WHERE SALARY >200 AND EID IN ( SELECT MGR FROM EMPLOYE WHERE MGR IS NOT NULL));

Q62.SELECT THE EMPLOYEES WHOSE DNAME IS HAVING AT LEAST TWO 'E' IN IT.
            SELECT ENAME FROM EMPLOYE WHERE DEPTNO IN(SELECT DEPTNO FROM DEPT WHERE DNAME LIKE '%E%E%');

Q63.DISPLAY ENAME,SAL OF EMPLOYEES WHO ARE EARNING MORE THAN ANY OF THE ANALYST.
           SELECT ENAME,SALARY FROM EMPLOYE WHERE SALARY > ANY (SELECT SALARY FROM EMPLOYE WHERE DESIGNATION='ANALYST');

Q64.SELECT ALL THE EMPLOYEES WHO ARE WORKING FOR CHICAGO
          SELECT ENAME FROM EMPLOYE WHERE DEPTNO IN(SELECT DEPTNO FROM DEPT WHERE LOCATION='CHICAGO');

Q65. QUERY TO DISPLAY EMPLOYEE NAMES WHO IS HAVING MINIMUM SALARY IN DEPARTMENT RESEARCH.
           SELECT ENAME FROM EMPLOYE WHERE SALARY IN(SELECT MIN(SALARY) FROM EMPLOYE WHERE DEPTNO IN(SELECT DEPTNO FROM DEPT WHERE DNAME='RESEARCH'));

Q66.LIST THE DEPARTMENT NAMES THAT ARE HAVING SALESMAN.
          SELECT DNAME FROM DEPT WHERE DEPTNO IN(SELECT DEPTNO FROM EMPLOYE WHERE DESIGNATION='SALESMAN');

Q67. LIST THE DEPARTMENT NAMES THAT ARE HAVING AT LEAST 3 EMPLOYES IN IT.
          SELECT DNAME FROM DEPT WHERE DEPTNO IN(SELECT DEPTNO FROM EMPLOYE GROUP BY DEPTNO HAVING COUNT(*)>=3);

Q68.LIST EMPLOYEES FROM RESEARCH AND ACCOUNTING DEPARTMENT HAVING ATLEAST TWO REPORTING
          SELECT ENAME FROM EMPLOYE WHERE DEPTNO IN (SELECT DEPTNO FROM DEPT WHERE DNAME='RESEARCH' AND DNAME='ACCOUNTING') AND EID IN (SELECT MGR FROM EMPLOYE GROUP BY MGR HAVING COUNT(*)>=2);

Q69.WRITE A QUERY TO DISPLAY EMPLOYEE NAME, JOB,LOCATION OF ALL EMPLOYEES WHO ARE WORKING AS MANAGER AND WORKS AT
CHICAGO.
         SELECT ENAME,DESIGNATION,LOCATION FROM EMPLOYE WHERE DESIGNATION IN ‘MANAGER’ AND DEPTNO IN (SELECT DEPTNO FROM DEPT WHERE LOCATION IN ‘CHICAGO’);

Q70. SELECT ENAME OF EMPLOYEE WHO EARNS 2ND MAX SALARY AND WORKS FOR LOCATION DALLAS.
        SELECT ENAME FROM EMPLOYE WHERE SALARY<(SELECT MAX(SALARY) FROM EMPLOYE WHERE SALARY<(SELECT MAX(SALARY) FROM EMPLOYE WHERE DEPTNO IN(SELECT DEPTNO FROM DEPT WHERE LOCATION='DALLAS')));

Q71. WRITE A QUERY TO DISPLAY THE EMPLOYEE INFORMATION WHO IS NOT TAKING COMMISSION AND JOINED COMPANY AFTER JULY 83.
        SELECT * FROM EMPLOYE WHERE COMM IS NULL AND HIREDATE>'31-JUL-83';

Q72.LIST EMPLOYEES FROM SALES AND RESEARCH DEPARTMENT HAVING ATLEAST 2 REPORTING EMPLOYEES .
       SELECT ENAME FROM EMPLOYE WHERE DEPTNO IN(SELECT DEPTNO FROM DEPT WHERE DNAME='SALES' AND DNAME='RESEARCH') AND EID IN(SELECT MGR FROM EMPLOYE GROUP BY MGR HAVING COUNT(*)>=2);

Q73.LIST EMPLOYEES WHO HAVE COMMISSION GREATER THAN MAXIMUM SALARY OF ALL THE SALESMAN AND WHO DO NOT REPORT
TO KING DIRECTLY .
       SELECT ENAME FROM EMPLOYE WHERE COMM>(SELECT MAX(SALARY) FROM EMPLOYE WHERE DESIGNATION='SALESMAN') AND MGR IN(SELECT EMPNO FROM EMPLOYE WHERE MGR NOT IN (SELECT EID FROM EMPLOYE WHERE ENAME='KING'));

Q74.DISPLAY THE LOCATION OF ALL THE DEAPRTMENTS WHICH HAVE EMPLOYEES JOINED IN THE YEAR 81
         SELECT LOCATION FROM DEPT WHERE DEPTNO IN(SELECT DEPTNO FROM EMPLOYE WHERE HIREDATE LIKE '%81');

Q75.DISPLAY DEPARTMENT WISE MINIMUM SALARY WHICH IS LESS THAN AVERAGE SALARY OF EMPLOYEES.
          SELECT MIN(SALARY),DEPTNO FROM EMPLOYE WHERE SALARY<(SELECT AVG(SALARY) FROM EMPLOYE) GROUP BY DEPTNO;

Q76.DISPLAY ALL THE EMPLOYEES WHO ARE REPORTING TO 'JONES'.
           SELECT ENAME FROM EMPLOYE WHERE MGR IN(SELECT EID FROM EMPLOYE WHERE ENAME='JONES');

Q77.DISPLAY ALL THE EMPLOYEE INFORMATION WHO ARE LIVING IN A LOCATION WHICH IS HAVING AT LEAST 2 'O' IN IT.
            SELECT * FROM EMPLOYE WHERE DEPTNO IN(SELECT DEPTNO FROM DEPT WHERE LOCATION LIKE '%O%O%');

Q78.DISPLAY THE NAMES OF EMPLOYEE FROM DEPARTMENT NUMBER 10 WITH SALARY GREATER THAN THAT OF ALL EMPLOYEE WORKING IN OTHER DEPARTMENTS.
             SELECT ENAME FROM EMPLOYE WHERE DEPTNO=10 AND SALARY> ALL (SELECT SALARY FROM EMPLOYE WHERE DEPTNO<>10);

Q79.DISPLAY THE NAMES OF EMPLOYEES WHO EARN HIGHEST SALARY IN THEIR RESPECTIVE JOBS.
            SELECT ENAME FROM EMPLOYE WHERE SALARY IN (SELECT MAX(SALARY) FROM EMPLOYE GROUP BY DESIGNATION );

Q80. DISPLAY THE EMPLOYEE NUMBER AND NAME OF EMPLOYEE WORKING AS CLERK AND EARNING HIGHEST SALARY AMONG CLERKS.
            SELECT ENAME,EID FROM EMPLOYE WHERE DESIGNATION='CLERK' AND SALARY IN(SELECT MAX(SALARY) FROM EMPLOYE WHERE DESIGNATION='CLERK');

Q81. WRITE A QUERY TO FIND SMITH'S MANAGER'S MANAGER HIREDATE.
             SELECT HIREDATE  FROM EMPLOYE WHERE EID IN(SELECT MGR FROM EMPLOYE WHERE EID IN(SELECT MGR
FROM EMPLOYE WHERE ENAME='SMITH'));

Q82.LIST THE NUMBER OF EMPLOYEES WHOSE JOB IS SALESMAN WORKING FOR NEWYORK AND CHICAGO
             SELECT COUNT(*) FROM EMPLOYE WHERE DESIGNATION='SALESMAN' AND DEPTNO IN(SELECT DEPTNO FROM DEPT WHERE LOCATION IN ('NEW YORK' ,'CHICAGO'));

Q83. LIST THE DEPARTMENT NAMES IN WHICH THE EMPLOYEES ARE HIRED BETWEEN 1ST OF JAN 1981 AND 31ST DEC 1982 WITH SALARY
MORE THAN 1800.
             SELECT DNAME FROM DEPT WHERE DEPTNO IN(SELECT DEPTNO FROM EMPLOYE WHERE HIREDATE BETWEEN '01-JAN-81' AND '31-DEC-82' AND SALARY>1800);

Q84.DISPLAY LOCATION OF THE EMPLOYEE WHO EARN MAXIMUM SALARY AND HAVE NO REPORTING MANAGER
             SELECT LOCATION FROM DEPT WHERE DEPTNO IN(SELECT DEPTNO FROM EMPLOYE WHERE SALARY IN(SELECT MAX(SALARY) FROM EMPLOYE WHERE MGR IS NULL));

Q85. LIST EMPLOYEES WHO WORKS FOR ACCOUNTING DEPARTMENT AND THEIR SALARY GREATER THAN AVERAGE SALARY OF THEIR
DEPARTMENT
              SELECT ENAME FROM EMPLOYE WHERE DEPTNO IN(SELECT DEPTNO FROM DEPT WHERE DNAME='ACCOUNTING') AND DEPTNO IN(SELECT DEPTNO FROM EMPLOYE WHERE SALARY>(SELECT AVG(SALARY) FROM EMPLOYE));

Q86. DISPLAY LOCATION OF THE EMPLOYEE WHO EARN COMMISSION
               SELECT LOCATION FROM DEPT WHERE DEPTNO IN(SELECT DEPTNO FROM EMPLOYE  WHERE COMM IS NOT NULL);

Q87.LIST THE EMPLOYEES WHO DOES NOT DIRECTLY REPORT TO PRESIDENT,HAVE COMMISSION AND SALARY MORE THAN MAX
SALARY OF ALL THE CLERK WITHOUT USING NULL OR NOT NULL
               SELECT ENAME FROM EMPLOYE WHERE MGR IN(SELECT EID FROM EMPLOYE WHERE MGR IN(SELECT EID FROM EMPLOYE
WHERE DESIGNATION='PRESIDENT')) AND COMM >=0 AND SALARY>(SELECT MAX(SALARY) FROM EMPLOYE WHERE DESIGNATION='CLERK');

Q88.DISPLAY ALL THE EMPLOYEES WHOSE SALARY IS GREATER THAN AVG SAL OF DEPARTMENT 20
                  SELECT ENAME FROM EMPLOYE WHERE SALARY>(SELECT AVG(SALARY) FROM EMPLOYE WHERE DEPTNO=20);

Q89. LIST THE EMPLOYEE DEPTNAME AND LOC OF ALL THE EMPLOYEES WHO ARE CLERK ,REPORTING TO BLAKE AND SALARY IS LESSER THAN MARTIN SALARY
                  SELECT DNAME,LOCATION FROM DEPT WHERE DEPTNO IN(SELECT DEPTNO FROM EMPLOYE WHERE DESIGNATION='CLERK' AND MGR IN(SELECT EID FROM EMPLOYE WHERE ENAME='BLAKE' )AND SALARY<(SELECT SALARY FROM EMPLOYE WHERE ENAME='MARTIN'));

Q90.DISPLAY LOC AND DNAME WHOSE JOB IS MANAGER AND HAS SAL LESS THAN CLERK.
                  SELECT LOCATION,DNAME FROM DEPT WHERE DEPTNO IN(SELECT DEPTNO FROM EMPLOYE WHERE DESIGNATION='MANAGER' AND SALARY < (SELECT SALARY FROM EMPLOYE WHERE DESIGNATION='CLERK'));

Q91.DISPLAY EMPLOYEES LOCATION WHO HAS SOME COMMISSION.
                   SELECT LOCATION FROM DEPT WHERE DEPTNO IN(SELECT DEPTNO FROM EMPLOYE WHERE COMM IS NOT NULL);

Q92.DISPLAY EMPNO, ENAME, JOB, WHOSE JOB HAS 'E' IN IT AND DISPLAY EMPNO IN DESCENDING ORDER.
                    SELECT EID,ENAME,DESIGNATION FROM EMPLOYE WHERE DESIGNATION LIKE '%E%' ORDER BY EID DESC;

Q93. DISPLAY DNAME, LOC,DEPTNO OF EMPLOYEES WHO HAS SAME REPORTING MANAGER???
                    SELECT DNAME,LOCATION,DEPTNO FROM DEPT WHERE DEPTNO IN(SELECT DEPTNO FROM EMPLOYE
WHERE EID IN(SELECT MGR FROM EMP GROUP BY MGR HAVING COUNT(*)>1));

Q94. DISPLAY AVG SALARY OF ALL EMPLOYEES WHOSE DEPT NAME IS ACCOUNTING???
                   SELECT AVG(SALARY) FROM EMPLOYE WHERE DEPTNO IN(SELECT DEPTNO FROM DEPT WHERE DNAME='ACCOUNTING');

Q95. DISPLAY ALL EMPLOYEES DETAILS WHOSE HIREDATE IS IN YEAR 81???
                   SELECT * FROM EMPLOYE WHERE HIREDATE LIKE '%81';

Q96. DISPAY DETAILS OF SMITH AND EMPLOYEES WORKING AS PRESIDENT ALONG WITH HIKE OF 35% IN SALARY.
                  SELECT SALARY,SALARY+SALARY*35/100 HIKE FROM EMPLOYE WHERE ENAME='SMITH' OR DESIGNATION='PRESIDENT';

Q97. DISPLAY NUMBER OF EMPLOYEES WHOSE COMMISSION IS MORE THAN SALARY.
                  SELECT COUNT(*) FROM EMPLOYE WHERE COMM > ( SELECT SALARY FROM EMPLOYE);

Q98. LIST THE EMPLOYEES WHOSE DAILY SALARY IS GREATER THAN 1500 AND WHO ARE JOINED BEFORE 1982 ONLY.
                   SELECT ENAME FROM EMPLOYE WHERE SALARY/30 >1500 AND HIREDATE <'01-JAN-82';

Q99. DISPLAY NUMBER OF EMPLOYEES WHOSE COMMISSION IS MORE THAN SALARY.
                    SELECT COUNT(*) FROM EMPLOYE WHERE COMM >(SELECT SALARY FROM EMPLOYE);

Q100. LIST THE EMPLOYEES WHOSE DAILY SALARY IS GREATER THAN 1500 AND WHO ARE JOINED BEFORE 1982 ONLY.
                    SELECT ENAME FROM EMPLOYE WHERE SALARY/30 >1500 AND HIREDATE <'01-JAN-82';

Q101. DISPLAY ALL THE EMPLOYEES WHOSE JOB SAME AS 'SMITH' AND DEPARTMENT SAME AS 'JONES' AND SALARY MORE THAN 'TURNER'
                    SELECT ENAME FROM EMPLOYE WHERE DESIGNATION IN(SELECT DESIGNATION FROM EMPLOYE WHERE ENAME='SMITH') AND DEPTNO IN(SELECT DEPTNO FROM DEPT WHERE DEPTNO IN(SELECT DEPTNO FROM EMPLOYE WHERE ENAME='JONES')) AND SALARY>(SELECT SALARY FROM EMPLOYE WHERE ENAME='TURNER');

Q102. DISPLAY ALL THE EMPLOYEES WHOSE NAME START WITH 'S' AND HAVING SALARY MORE THAN 'ALLEN' AND LESS THAN FORD
                   SELECT ENAME FROM EMPLOYE WHERE ENAME LIKE 'S%' AND SALARY>(SELECT SALARY FROM EMPLOYE WHERE ENAME='ALLEN') AND SALARY<(SELECT SALARY FROM EMPLOYE WHERE ENAME='FORD');

103. DISPLAY ALL THE CLERKS AND ANALYST WHO ARE NOT WORKING FOR 'DALLAS'
                   SELECT DESIGNATION FROM EMPLOYE WHERE DESIGNATION IN ('CLERK','ANALYST') AND DEPTNO NOT IN(SELECT DEPTNO FROM DEPT WHERE LOCATION='DALLAS');

104. DISPLAY DEPARTMENT NAME WHICH IS HAVING AT LEAST ONE 'MANAGER'
                  SELECT DNAME FROM DEPT WHERE DEPTNO IN(SELECT DEPTNO FROM EMPLOYE WHERE DESIGNATION='MANAGER' GROUP BY DESIGNATION HAVING COUNT(*)>=1);

105. DISPLAY MAXIMUM SALARY OF 'SALES' DEPARTMENT
                 SELECT MAX(SALARY) FROM EMPLOYE WHERE DEPTNO IN(SELECT DEPTNO FROM DEPT WHERE DNAME='SALES');

106. DISPLAY THE 2ND MAXIMUM SALARY
                 SELECT MAX(SALARY) FROM EMPLOYE WHERE SALARY<(SELECT MAX(SALARY) FROM EMPLOYE);

107.DISPLAY THE DEPT NAME OF THE EMP WHO GETS 3RD MAXIMUM SALARY
                SELECT DNAME FROM DEPT WHERE DEPTNO IN(SELECT DEPTNO FROM EMPLOYE WHERE SALARY=(SELECT MAX(SALARY) FROM EMPLOYE WHERE SALARY<(SELECT MAX(SALARY) FROM EMPLOYE WHERE SALARY<(SELECT MAX(SALARY) FROM EMPLOYE))));

108. DISPLAY ALL THE EMPLOYEES WHO ARE EARNING MORE THAN ALL THE MANAGERS(JOB).
                  SELECT ENAME FROM EMPLOYE WHERE SALARY> ALL (SELECT SALARY FROM EMPLOYE WHERE DESIGNATION='MANAGER');

109. DISPLAY ALL THE EMPLOYEES WHO ARE EARNING MORE THAN ANY OF THE MANAGER(JOB)
                   SELECT ENAME FROM EMPLOYE WHERE SALARY>ANY(SELECT SALARY FROM EMPLOYE WHERE DESIGNATION='MANAGER');

110. SELECT EMPNO, JOB AND SALARY OF ALL THE ANALYST WHO ARE EARNING MORE THAN ANY OF THE MANAGER(JOB)
                   SELECT EID,DESIGNATION,SALARY FROM EMPLOYE WHERE DESIGNATION='ANALYST' AND SALARY>ANY (SELECT SALARY FROM EMPLOYE WHERE DESIGNATION='MANAGER');

111. SELECT THE DEPARTMENT NAME AND LOCATION OF ALL THE EMPLOYEES WORKING FOR CLARK.
                   SELECT DNAME,LOCATION FROM DEPT WHERE DEPTNO IN(SELECT DEPTNO FROM EMPLOYE WHERE MGR IN
(SELECT EID FROM EMPLOYE WHERE ENAME='CLARK');

112. SELECT ALL THE EMPLOYEES WORKING FOR DALLAS
                    SELECT ENAME FROM EMPLOYE WHERE DEPTNO IN(SELECT DEPTNO FROM DEPT WHERE LOCATION='DALLAS');

113. DISPLAY ALL THE EMPLOYEES WHOSE SALARY IS GREATER THAN AVG SAL OF DEPARTMENT 20
                   SELECT ENAME FROM EMPLOYE WHERE SALARY>(SELECT AVG(SALARY) FROM EMPLOYE WHERE DEPTNO=20);

114.DISPLAY ALL THE EMPLOYEES WHO GETS MAXIMUM SALARY.
                  SELECT ENAME FROM EMPLOYE WHERE SALARY IN(SELECT MAX(SALARY) FROM EMPLOYE);

115.DISPLAY FIRST EMPLOYEE RECORD BASED ON HIREDATE
                   SELECT * FROM EMPLOYE WHERE HIREDATE IN(SELECT MIN(HIREDATE) FROM EMPLOYE);



